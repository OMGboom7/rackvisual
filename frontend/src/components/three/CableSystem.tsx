import type { Cable, RackComponent, Rack } from '../../types';

interface Props {
  cables: Cable[];
  components: RackComponent[];
  rack: Rack;
}

export default function CableSystem(_props: Props) {
  return null;
}
